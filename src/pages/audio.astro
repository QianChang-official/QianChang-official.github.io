---
import AudioConsole from '@components/audio/AudioConsole';
import HomeSider from '@components/layout/HomeSider.astro';
import Cover from '@components/ui/cover/Cover.astro';
import { CONTENT_PADDING } from '@constants/layout';
import { siteConfig } from '@constants/site-config';
import Layout from '@layouts/Layout.astro';
import TwoColumnLayout from '@layouts/TwoColumnLayout.astro';

const title = `音源控制台 | ${siteConfig.title}`;
const description =
  'R2 Audio Workspace 音源控制台：基于 Cloudflare R2 + Web Audio DSP 的在线音源工作台，支持 10 段 EQ、差分环绕声、频谱可视化、上传与管理。';
---

<Layout title={title} description={description}>
  <TwoColumnLayout showPostFooter={false}>
    <Cover slot="cover" title="音源控制台" />
    <HomeSider slot="sider" />
    <div class={`bg-gradient-start shadow-box flex flex-col gap-8 ${CONTENT_PADDING.standard}`}>
      <div class="mt-4 mb-4">
        <AudioConsole client:only="react" />
      </div>
    </div>
  </TwoColumnLayout>
</Layout>
